apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "art.fullname" . }}-test-connection"
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "art.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "art.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
